# Listing 6-7: Use `Include` to bring in relational data

The ability to get relational data relies on using `Include`.  Behind the scenes, the code that is generated by EF uses a join.

## The Method Declaration

Retrieve Items from the database and include the associated Category

1. The final method code follows:

    Find the code for the `GetAllItemsAsync_IncludeCategory` method and update to match the following code:

    ```cs
    private async Task<List<Item>> GetAllItemsAsync_IncludeCategory()
    {
        return await _db.Items.Include(x => x.Category)
                        .ToListAsync();
    }
    ```  

## Conclusion

This concludes Listing 6-7.