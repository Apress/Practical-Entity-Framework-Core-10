# Listing 6-18: Get all current Genres

Use the T-SQL command to get all Genres

## The Query

You may need to be more explicit, but you should be able to get the Genres by running the following T-SQL query in SSMS.

```sql
SELECT * FROM GENRES
```  